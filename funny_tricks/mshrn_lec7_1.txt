первая строчка: удалится p, тк выражение парсится как delete p <оператор запятая> pp, поэтому delete применяется только к p;
вторая строчка: удалится pp, тк оператор запятая возвращает значение того expr, которое находится справа => (p, pp) - это pp, delete применяется к pp;
Мораль:
  Не пишите код с таким ужасным синтаксисом!
