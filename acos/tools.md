# 🐧 Полезные Linux-команды для диагностики(АКОС ВШПИ ИЛЬЯ МЕЩЕРИН 2025)


### `ltrace`
**Трассировка библиотечных вызовов**  
*Отслеживает вызовы библиотечных функций (libc, libstdc++ и др.) во время выполнения программы*
- `-c` - статистика по вызовам (количество, время выполнения)
- `-p PID` - трассировка работающего процесса
- `-C` - декодирование C++ имён (demangle)
- `-f` - трассировка дочерних процессов (fork)
- `-o filename` - сохранить вывод в файл
### `strace`
**Трассировка системных вызовов**  
*Показывает взаимодействие программы с ядром ОС через syscalls (read, write, open и т.д.)*
- `-e trace=file` - только файловые операции (open, close, read, write)
- `-e trace=network` - только сетевые операции
- `-e trace=process` - операции с процессами (fork, exec, exit)
- `-p PID` - подключиться к существующему процессу
- `-c` - статистика системных вызовов
- `-f` - трассировка дочерних процессов
- `-o filename` - сохранить вывод в файл


### `pgrep`
**Поиск PID по имени процесса**  
*Быстрый поиск идентификаторов процессов по имени (удобнее чем `ps | grep`)*
- `-u user` - процессы конкретного пользователя
- `-l` - показать имена процессов вместе с PID
- `-f` - искать в полной командной строке
- `-x` - точное совпадение имени
- `-n` - только самый новый процесс
- `-o` - только самый старый процесс

### `ps`
**Информация о процессах**  
*Детальная информация о запущенных процессах (snapshot состояния системы)*
- `aux` - все процессы с детализацией (BSD-стиль)
- `-eo pid,ppid,cmd,%cpu,%mem` - кастомный формат вывода
- `-ef` - все процессы в иерархическом виде (Unix-стиль)
- `-p PID` - информация о конкретном процессе
- `--forest` - древовидное отображение родительских связей


### `man [раздел] [команда]`
**Руководство по командам**  
*Доступ к официальной документации Unix/Linux (manual pages)*
- `man 1` - пользовательские команды
- `man 2` - системные вызовы (open, read, fork и т.д.)
- `man 3` - библиотечные функции (printf, malloc и т.д.)
- `man 7` - различные соглашения и протоколы
- `-k keyword` - поиск по ключевому слову во всех man-страницах


### `echo $?`
**Код возврата последней команды**  
*Проверка успешности выполнения команды: 0 = успех, 1-255 = ошибка*
*Код возврата хранится в специальной переменной `$?` и обновляется после каждой команды*

### `ulimit`
**Управление лимитами ресурсов**  
*Ограничения ресурсов для текущей shell-сессии и её дочерних процессов*
- `-a` - показать все лимиты
- `-s` - размер стека (stack size) в КБ
- `-c` - размер core-дампов (0 = отключить)
- `-n` - максимальное количество открытых файлов
- `-u` - максимальное количество процессов пользователя
- `-v` - максимальный размер виртуальной памяти


### `objdump`
**Дизассемблирование и анализ**  
*Просмотр объектных файлов и исполняемых файлов (поддержка разных форматов)*
- `-d` - дизассемблирование только кода (.text секции)
- `-D` - дизассемблирование всех секций
- `-h` - заголовки секций (.text, .data, .bss и т.д.)
- `-t` - таблица символов
- `-S` - дизассемблирование с исходным кодом (если есть debug info)
- `-M intel` - использовать Intel синтаксис (вместо AT&T)

### `hexdump`
**Просмотр файлов в hex**  
*Шестнадцатеричный дамп файла (полезно для бинарных файлов)*
- `-C` - canonical hex+ASCII вывод (самый читаемый формат)
- `-n N` - ограничить вывод N байтами
- `-s offset` - начать с указанного смещения
- `-v` - показать все данные (без сжатия повторов)

### `readelf`
**Анализ ELF-файлов**  
*Детальная информация о ELF-бинарниках (Linux executable format)*
- `-h` - заголовок ELF-файла (entry point, архитектура и т.д.)
- `-l` - program headers (сегменты для загрузчика)
- `-S` - section headers (секции .text, .data, .bss и т.д.)
- `-s` - таблица символов (функции, переменные)
- `-d` - динамическая секция (зависимости от библиотек)
- `-a` - вся доступная информация (all) 

### `g++ file.cpp`
**Сборка C++ файла**
*Компилятор GNU C++ проходит 4 этапа: препроцессинг → компиляция → ассемблирование → линковка*
- `-E` - только препроцессинг (раскрывает макросы, включает заголовки)
- `-S` - препроцессинг + компиляция в ассемблер (.s файл)
- `-c` - создать объектный файл .o (без линковки)
- `-o filename` - имя выходного файла
- `-g` - добавить отладочную информацию для gdb
- `-O0/-O1/-O2/-O3` - уровни оптимизации (0 = без оптимизаций)
- `-Wall -Wextra` - включить предупреждения
- `-std=c++17` - стандарт C++ (c++11/14/17/20/23)
- `-static` - статическая линковка (включить все библиотеки в бинарник)
- `-shared` - создать shared library (.so файл)
- `-fPIC` - Position Independent Code (нужно для shared libraries)
- `-llibname` - линковать с библиотекой libname.so или libname.a
- `-Lpath` - добавить путь для поиска библиотек при линковке
- `-Ipath` - добавить путь для поиска заголовочных файлов
- `-Wl,-rpath,/path` - добавить путь для поиска shared libraries во время выполнения
### `c++filt`
**Декодирование C++ символов**  
*Преобразует mangled имена C++ в читаемый вид*
- Пример: `_Z3addii` → `add(int, int)`
- Использование: `c++filt _Z3addii` или `nm file.o | c++filt`
- Компиляторы C++ используют name mangling для поддержки перегрузки функций
- Полезно при анализе `nm`, `objdump`, `readelf` вывода

### `ldd`
**Список динамических зависимостей**  
*Показывает какие shared libraries (.so) нужны исполняемому файлу*
- `ldd ./program` - вывод всех зависимостей с путями
- Показывает где линкер нашёл библиотеку (или "not found")
- Использует `LD_LIBRARY_PATH` и системные пути (/lib, /usr/lib)
- **Внимание:** не запускать на недоверенных файлах (может выполнить код!)

### `LD_LIBRARY_PATH` и `LD_PRELOAD`
**Переменные окружения динамического линкера**  
*Управляют поведением загрузчика динамических библиотек*

**`LD_LIBRARY_PATH`**
- Список путей (через `:`) для поиска shared libraries
- Проверяется перед системными путями
- Пример: `export LD_LIBRARY_PATH=/home/user/libs:$LD_LIBRARY_PATH`
- Удобно для тестирования новых версий библиотек

**`LD_PRELOAD`**
- Список библиотек для загрузки перед всеми остальными
- Позволяет переопределить системные функции (hooking)
- Пример: `LD_PRELOAD=./my_malloc.so ./program`
- Используется для debugging, profiling, перехвата вызовов

### `ld`
**Линкер GNU**  
*Низкоуровневый линкер для объединения объектных файлов*
- Обычно вызывается через `g++`, не напрямую
- `ld -o program file1.o file2.o -lc` - базовая линковка
- `-L/path` - добавить путь поиска библиотек
- `-l library` - линковать с библиотекой
- `-dynamic-linker /lib64/ld-linux-x86-64.so.2` - указать динамический линкер
- `-rpath /path` - встроить путь для runtime поиска библиотек
- `-M` - вывести memory map (какие секции куда попали)